import ImgInputDirective from './img-input.directive';

describe('ImgInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgInputDirective();
    expect(directive).toBeTruthy();
  });
});
